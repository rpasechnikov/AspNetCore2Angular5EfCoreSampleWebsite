
/*
* This project uses ASP.NET Core, Entity Framework Core, Angular 5.0 for its codebase
* It also uses Nuget and Bower for package management. When getting this project for the 
* first time, make sure to run:
*
* npm install to install all necessary npm packages
* restore all bower packages (VS 2017 does that automatically as part of the build process)
* ng build to build the angular application
*/